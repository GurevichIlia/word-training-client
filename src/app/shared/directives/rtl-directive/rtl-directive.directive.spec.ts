import { RtlDirectiveDirective } from './rtl-directive.directive';

describe('RtlDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RtlDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
