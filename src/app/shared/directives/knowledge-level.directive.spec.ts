import { KnowledgeLevelDirective } from './knowledge-level.directive';

describe('KnowledgeLevelDirective', () => {
  it('should create an instance', () => {
    const directive = new KnowledgeLevelDirective();
    expect(directive).toBeTruthy();
  });
});
